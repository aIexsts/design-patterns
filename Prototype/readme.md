``````
Prototype: 

Copy object instead of creating

Specify the kinds of objects to create using a prototypical instance
and create new objects by copying its prototype

Participants:

Prototype (Graphic)
ConcretePrototype (Staff, WholeNote, HalfNote)

Client (GraphicTool)

``````

``````
Example:

Copy state of prototype instead of creating new object
Editors ctrl c -> ctrl v complex objects

``````

